<% include ../partials_admin/header %>

    <div class="container">
        <div class="section">
            <div class="row">
              <h2>Diseases List</h2>
              <!-- coding here -->
              <table class="striped">
                <thead>
                  <tr>
                      <th>Name</th>
                      <th>Descriptions</th>
                      <th>Solutions</th>
                      <th>Edit</th>
                      <th>Delete</th>
                  </tr>
                </thead>

                <tbody>
                  <% penyakit.forEach( list => { %>
                    <tr>
                      <td><%= list.name %></td>
                      <td><%= list.deskripsi.slice(0,40)+"..." %></td>
                      <td><%= list.solusi.slice(0,40)+"..." %></td>
                      <td><a href="edit_penyakit/<%= list.id %>">Edit</a></td>
                      <td><a id="delete" onclick="return confirm('Are you sure?')" href="delete_penyakit/<%= list.id %>">Delete</a></td>
                    </tr>
                  <% }) %>
                </tbody>
              </table>

              <a href='add_disease' class="waves-effect waves-light btn"><i class="material-icons left">assignment</i>ADD</a>
            </div>
        </div>
    </div>


<% include ../partials_admin/footer %>
